POSTGRES_USER=resume
POSTGRES_PASSWORD=changeme
POSTGRES_DB=resume

JWT_SECRET=changeme

OLLAMA_MODEL=llama3.2

# Laptop dev only — omit or leave unset on homelab
DEV_PORT=8080
RESTART_POLICY=no

# Set to true on homelab with a capable model (llama3.1:8b or larger)
AGENTIC_PARSE=false
